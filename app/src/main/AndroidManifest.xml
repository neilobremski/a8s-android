<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- SMS (send and receive) -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Phone -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Contacts — needed to resolve RCS notification titles (contact display
         names like "Neil C. Obremski") back to phone numbers so the
         existing phonebook lookup works. SMS path doesn't need this; the
         system gives us the raw phone number directly there. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Battery optimization exemption -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Exact alarm — required for the 5-minute keep-alive self-recovery alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Notification Listener — required to intercept Google Messages (RCS) notifications. -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- /update — installs APK updates fetched from GitHub Releases.
         User must enable "Install unknown apps" for our package once, in
         Settings; the manifest declaration alone isn't enough. The app
         can't silently install on stock Android; the system shows a
         confirmation dialog the user taps. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- /screenshot — MediaProjection requires a foreground service of
         type mediaProjection (declared above) plus this permission since
         API 34. The user must grant projection access via the system
         dialog the first time; we cache the token for the service's
         lifetime. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- /photo, /video — Camera2 capture from the foreground service. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <!-- /video, future audio capture — record audio alongside video. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- /location — one-shot fix via FusedLocationProvider (if available)
         or LocationManager fallback. Coarse alone is enough for an
         approximate fix; fine is requested for higher accuracy. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Scoped media access (API 33+) for /ls and /cat over media
         directories. We deliberately skip WRITE_EXTERNAL_STORAGE
         (deprecated) — file writes use cacheDir / app-specific dirs. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".A8sAndroid"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="a8s Android"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".A8sService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|mediaProjection|microphone|camera|location" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".SmsNotificationListener"
            android:exported="true"
            android:label="a8s SMS/RCS Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Accessibility service for /tap, /swipe, /key, /input, /find,
             and the macro batch runner. The user must enable it manually
             in Settings - Accessibility - accessibility access is
             special-permission and cannot be granted via the runtime
             permission dialog. The MainActivity "Enable Accessibility
             Service" button jumps to that page. -->
        <service
            android:name=".A11yService"
            android:exported="true"
            android:label="a8s Automation"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- FileProvider exposes the downloaded APK to the system installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
